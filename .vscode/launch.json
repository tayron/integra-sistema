{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "remotePath": "${workspaceFolder}",
            "mode": "remote",
            "port": 3306,
            "host": "127.0.0.1",
            "program": "${workspaceFolder}",
    }
    ]    
}